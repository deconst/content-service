---
content:
  build: .
  ports:
  - "9000:8080"
  environment:
    RACKSPACE_USERNAME:
    RACKSPACE_APIKEY:
    RACKSPACE_REGION: DFW
    CONTENT_CONTAINER: content-service-dev
    ASSET_CONTAINER: assets-dev
    CONTENT_LOG_LEVEL: debug
